{
  "name": "functions",
  "version": "1.0.0",
  "description": "Firebase Cloud Functions for TWICE Proxy Buying Service",
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^7.0.5"
  },
  "devDependencies": {
    "@types/node": "^20.19.32",
    "typescript": "^5.9.3"
  },
  "private": true
}
